{
    "url": "https://docs.snowflake.com/en/developer-guide/snowpark/reference/python/1.26.0/modin/pandas_api/modin.pandas.Series.items",
    "title": "modin.pandas.Series.items | Snowflake Documentation",
    "paragraphs": [
        "Lazily iterate over (index, value) tuples.",
        "This method returns an iterable tuple (index, value). This is convenient if you want\nto create a lazy iterator.",
        "Iterable of tuples containing the (index, value) pairs from a Series.",
        "Iterable",
        "See also",
        "Iterate over (column name, Series) pairs.",
        "Iterate over DataFrame rows as (index, Series) pairs.",
        "Notes",
        "Iterating over rows is an antipattern in Snowpark pandas and pandas. Use Series.apply() or other aggregation\nmethods when possible instead of iterating over a Series. Iterators and for loops do not scale well.",
        "You should never modify something you are iterating over. This will not work. The iterator returns a copy\nof the data and writing to it will have no effect.",
        "Examples",
        "Was this page helpful?"
    ]
}