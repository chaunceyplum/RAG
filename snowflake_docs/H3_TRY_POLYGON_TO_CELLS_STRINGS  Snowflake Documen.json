{
    "url": "https://docs.snowflake.com/en/sql-reference/functions/h3_try_polygon_to_cells_strings",
    "title": "H3_TRY_POLYGON_TO_CELLS_STRINGS | Snowflake Documentation",
    "paragraphs": [
        "Geospatial functions",
        "A special version of H3_POLYGON_TO_CELLS_STRINGS that returns NULL if an error\noccurs when it attempts to return an array of VARCHAR values of the\nhexadecimal IDs of H3 cells that have centroids contained by\na Polygon (specified by a GEOGRAPHY object).",
        "A GEOGRAPHY object that represents a Polygon.",
        "An INTEGER between 0 and 15 (inclusive) that specifies the H3 resolution that you want to use for the returned H3 cells.",
        "Specifying any other INTEGER value results in an error.",
        "Returns an array of VARCHAR values or NULL.",
        "If the function can perform a successful calculation, returns an array of VARCHAR values for the hexadecimal IDs\nof the H3 cells that have centroids contained in the specified input Polygon.",
        "If the function cannot perform a successful calculation, returns NULL without reporting an error.",
        "See H3_POLYGON_TO_CELLS_STRINGS for the usage notes.",
        "The following example attempts to return an array of VARCHAR values of the hexadecimal IDs of\nH3 cells that have centroids contained by a Polygon (specified by\na GEOGRAPHY object). Because the array with the cells that cover\nthe given hexagon at the given resolution exceeds the allowed size limit, the function returns NULL.",
        "For examples that successfully return an array of IDs, see H3_POLYGON_TO_CELLS_STRINGS.",
        "Was this page helpful?",
        "On this page",
        "Related content"
    ]
}