{
    "url": "https://docs.snowflake.com/en/sql-reference/functions/h3_try_coverage",
    "title": "H3_TRY_COVERAGE | Snowflake Documentation",
    "paragraphs": [
        "Geospatial functions",
        "A special version of H3_COVERAGE that returns NULL if an error occurs when it\nattempts to return an array of IDs (as INTEGER values) identifying the minimal\nset of H3 cells that completely cover a shape (specified by a\nGEOGRAPHY object).",
        "A GEOGRAPHY object.",
        "An INTEGER between 0 and 15 (inclusive) specifying the H3 resolution that you want to use for the returned H3 cells.",
        "Specifying any other INTEGER value results in an error.",
        "Returns an array of INTEGER values or NULL.",
        "If the function can perform a successful calculation, returns an array of INTEGER values for the IDs of\nthe minimal set of H3 cells that completely cover the specified input shape.",
        "If the function cannot perform a successful calculation, returns NULL without reporting an error.",
        "See H3_COVERAGE for the usage notes.",
        "The following example attempts to return an array of IDs that identify the minimal set of H3\ncells that completely cover a shape (specified by a GEOGRAPHY object). Because the array with\nthe cells that cover the given hexagon at the given resolution exceeds the allowed size limit, the function returns NULL.",
        "For examples that successfully return an array of IDs, see H3_COVERAGE.",
        "Was this page helpful?",
        "On this page",
        "Related content"
    ]
}